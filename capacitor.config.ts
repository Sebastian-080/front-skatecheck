
import type {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {

  appId: "com.logistica",
  appName: "logistica",
  webDir: ".dist/logistica/browser"
}

export default config;